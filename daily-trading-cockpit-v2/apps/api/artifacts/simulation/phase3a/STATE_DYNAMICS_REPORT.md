# State dynamics report

All dynamics are calibration-only and include sample size, effective sample size, calibration period, uncertainty, and support status in `results.json`. Funding mean reversion and mark-basis dynamics are explicitly UNSUPPORTED, not synthesized.
