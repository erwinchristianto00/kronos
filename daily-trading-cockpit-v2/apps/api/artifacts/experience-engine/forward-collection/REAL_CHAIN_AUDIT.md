# Real Chain Audit

Run `tsx scripts/audit-forward-causal-experience.ts` after a naturally resolved shadow order. The script reads only the append-only forward journal and writes `REAL_CHAIN_AUDIT.json`. It reports direct links, complete frozen-auditor chains, and a deterministic audit hash.

No real forward chain has been claimed by this local implementation. That requires staged observation on 3102 or 3101.
