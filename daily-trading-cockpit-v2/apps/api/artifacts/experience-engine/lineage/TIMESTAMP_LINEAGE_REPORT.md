# Timestamp lineage report

Required invariant: `decisionAsOfMs <= openedAtMs <= closedAtMs <= resolvedAtMs`.

```json
{
  "INCOMPLETE": 2884
}
```

The current real corpus has **0** complete chains. Processing timestamps were not substituted for missing market close/resolution timestamps.
