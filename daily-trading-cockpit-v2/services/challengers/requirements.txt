fastapi==0.115.14
uvicorn==0.35.0
pydantic==2.11.7
numpy>=1.26,<3
pandas>=2.2,<3
# Install torch separately from the official CPU-only index (see README). Using
# the default PyPI torch wheel on Linux can download an unnecessary CUDA runtime.
chronos-forecasting>=2.1,<3
timesfm[torch]>=2.0,<3
